1️⃣ Create EKS Cluster (Control Plane)
```bash
eksctl create cluster --name my-first-cluster --region ap-south-1 --version 1.30 --without-nodegroup


-----------------------------------------------------------------------------------------------------------------------

2️⃣ Associate OIDC Provider
eksctl utils associate-iam-oidc-provider --region ap-south-1 --cluster my-first-cluster --approve

------------------------------------------------------------------------------------------------------------------------
3️⃣ Create Node Group
eksctl create nodegroup \
  --cluster my-first-cluster \
  --region ap-south-1 \
  --name my-node-group \
  --node-type t3.medium \
  --nodes 2 \
  --nodes-min 2 \
  --nodes-max 2 \
  --ssh-access \
  --ssh-public-key aws-key \
  --managed
  ---------------------------------------------------------------------------------------------------------------------

4️⃣ Update Kubeconfig
aws eks update-kubeconfig --name my-first-cluster --region ap-south-1

------------------------------------------------------------------------------------------------------------------------

5️⃣ Deploy NGINX
kubectl create namespace nginx
kubectl apply -f nginx-deployment.yaml
kubectl get svc -n nginx

--------------------------------------------------------------------------------------------------------------------------
